import { ProfileSettings } from "@/components/dashboard/ProfileSettings";

export const metadata = { title: "Channel settings — TVinBio" };

export default function SettingsPage() {
  return <ProfileSettings />;
}
