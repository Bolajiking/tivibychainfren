import { StreamsManager } from "@/components/dashboard/StreamsManager";

export const metadata = { title: "Streams — TVinBio" };

export default function StreamsPage() {
  return <StreamsManager />;
}
